[phases.setup]
  nixPkgs = ["nodejs_22"]

  [phases.install]
  cmds = ["npm install -g pnpm && pnpm install --frozen-lockfile"]

  [phases.build]
  cmds = ["pnpm --filter @workspace/dobierzmibilet run build"]

  [start]
  cmd = "npx serve -s artifacts/dobierzmibilet/dist/public -l $PORT"
  